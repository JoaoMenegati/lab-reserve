import { StyleSheet } from "react-native";

const LoginStyle = StyleSheet.create({
  inputContainer: {
    paddingBottom: 45,
  },

  button: {
    height: 40,
    width: 250,
  },

  image: {
    height: 125,
    width: 125,
    paddingBottom: 10,
  },

  imageContainer: {
    alignItems: "center",
    paddingBottom: 55,
  },
});

export default LoginStyle;
