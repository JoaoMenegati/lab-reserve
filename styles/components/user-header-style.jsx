import { StyleSheet } from "react-native";

const UserHeaderStyle = StyleSheet.create({
  headerRow: {
    flexDirection: "row",
    justifyContent: "space-between",
  },

  imageProfile: {
    height: 60,
    width: 60,
    borderRadius: 64,
  },
});

export default UserHeaderStyle;
